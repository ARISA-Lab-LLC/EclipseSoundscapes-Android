<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="@dimen/margin_small"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Years"/>

    <TextView
        android:id="@+id/primary_time"
        android:layout_width="@dimen/countdown_time_size"
        android:layout_height="@dimen/countdown_time_size"
        android:layout_marginEnd="1dp"
        android:background="@drawable/round_countdown"
        android:gravity="center"
        android:fontFamily="sans-serif-smallcaps"
        android:textSize="@dimen/default_text_size"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toBottomOf="@+id/title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/secondary_time"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:text="0"/>

    <TextView
        android:id="@+id/secondary_time"
        android:layout_width="@dimen/countdown_time_size"
        android:layout_height="@dimen/countdown_time_size"
        android:layout_marginStart="1dp"
        android:background="@drawable/round_countdown"
        android:gravity="center"
        android:fontFamily="sans-serif-smallcaps"
        android:textSize="@dimen/default_text_size"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toBottomOf="@+id/title"
        app:layout_constraintStart_toEndOf="@+id/primary_time"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:text="0"/>

</androidx.constraintlayout.widget.ConstraintLayout>